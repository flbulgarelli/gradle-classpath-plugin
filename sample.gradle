buildscript {
    repositories {
        mavenLocal()
    }
    dependencies {
        classpath group: 'com.zauberlabs.gradle.plugins', name: 'classpath-plugin', version: '1.0-SNAPSHOT'
    }
}

apply plugin: 'classpath'
apply from: 'classpath://snippet.gradle'

